#include "lists.h"
/**
 * is_palindrome - function to know if the list is a palindrome
 * @head: pointer to pointer of first node of listint_t list
 * Return: 0 if its not a palindrome or 1 f it is
 */
int is_palindrome(listint_t **head)
{
	if (head->next)
	{
		return (0);
	}
	return (1)
}
